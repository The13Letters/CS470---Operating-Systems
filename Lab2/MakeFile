#Define the C compiler
CC = gcc

#Define any compile-time flags
CFLAGS = -Wall -Wextra -g

#Define the target executable
TARGET = processSimulator

#Default target
all: $(TARGET)

#Build target
$(TARGET): processSimulator.c
	$(CC) $(CFLAGS) -o $@ $^

#Remove compiled files
clean:
	rm -f $(TARGET)
